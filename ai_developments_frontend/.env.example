# Environment configuration for AI Developments Frontend (CRA)
# Copy this file to ".env" to run the app locally:
#   cp .env.example .env
#
# Note:
# - Create React App only exposes variables prefixed with REACT_APP_.
# - Mock mode avoids all external network calls and serves a local mock dataset.

# Data provider selection: HN | NEWSAPI | GNEWS
# HN requires no API key. NEWSAPI and GNEWS require keys below.
REACT_APP_DATA_PROVIDER=HN

# Force the app to use the local mock dataset and bypass all external APIs.
# Set to "true" to ensure zero network calls for data fetching.
REACT_APP_USE_MOCK=true

# Optional: Provider keys (only required when using NEWSAPI or GNEWS and mock mode is false)
REACT_APP_NEWSAPI_KEY=
REACT_APP_GNEWS_KEY=

# Optional metadata
REACT_APP_APP_NAME=AI Developments - Last 48 Hours (Frontend)
REACT_APP_VERSION=0.1.0
